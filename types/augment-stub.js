// Runtime stub for the types-only `bare-styled/types` entry. The real content
// is styled-components-augment.d.ts (loaded by TypeScript via the exports
// map's `types` condition); importing this at runtime is a harmless no-op.
'use strict'
module.exports = {}
