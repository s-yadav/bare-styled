// OBSOLETE. Tested the lazy CSS-variable descriptor (first-render flatten -> var
// / bail). The model is now hash-class; coverage moved to descriptor.test.js,
// patch.test.js, and pipeline.test.js. Delete on your machine:
//   rm test/e2e/lazyCreateStyled.test.js
test.skip('obsolete: lazy css-variable descriptor replaced by hash-class model', () => {})
